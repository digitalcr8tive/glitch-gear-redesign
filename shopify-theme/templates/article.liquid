<article class="gh-page gh-info-page"><header class="gh-page-heading"><span>GLITCH / NEWS</span><h1>{{ article.title }}</h1><p>{{ article.published_at | date: format: 'date' }}</p></header><div class="gh-richtext">{{ article.content }}</div></article>
