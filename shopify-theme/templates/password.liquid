<section class="gh-page gh-empty"><span>STORE / LOCKED</span><h1>{{ shop.name }}</h1><p>{{ shop.password_message }}</p>{% form 'storefront_password', class: 'gh-account-form' %}{{ form.errors | default_errors }}<label for="StorePassword">Store password</label><input id="StorePassword" type="password" name="password" autocomplete="current-password"><button class="gh-button gh-primary" type="submit">Enter store</button>{% endform %}</section>
